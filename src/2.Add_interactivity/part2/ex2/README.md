> React 컴포넌트 함수는 일반 함수처럼 한 번만 실행되는 게 아니라, 상태가 바뀔 때마다 다시 실행

## 일반 변수 (let)

- 컴포넌트 함수가 실행될 때마다 새로 초기화됨.

- 값이 바뀌어도 React가 신경 안 쓰기 때문에 리렌더링 트리거 X.

- 다음 렌더링 때는 초기값으로 돌아가 버림.

## useState 상태 변수

- React가 컴포넌트 바깥쪽(React의 내부 메모리)에 저장해둠.

- etState가 호출되면 React가 감지 → 자동으로 리렌더링 실행.

- UI에 최신 값이 반영.
